# Development / notebook-only dependencies (NOT needed by the deployed app).
# Install with:  pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

# Jupyter (Part 1 notebook)
jupyter==1.1.1
ipykernel==7.2.0
nbformat>=5.10

# Visualization (notebook EDA + viz)
matplotlib==3.10.9
seaborn==0.13.2

# Explainability (global feature importance in the notebook; predict.py does NOT
# require shap at serve time)
shap==0.52.0
