# Runtime dependencies for the retention agent, eval suite, and Streamlit demo.
# These are what the DEPLOYED app needs (Streamlit Cloud installs from this file).
# Versions pinned to what was used during development (Python 3.13).

# Web demo
streamlit==1.58.0

# Agent / LLM
anthropic==0.105.2
groq==0.10.0
python-dotenv==1.2.2

# Model loading + inference (the exported pipeline is scikit-learn; xgboost kept
# so the artifact still loads if a boosted model is selected on retrain)
pandas==3.0.3
numpy==2.4.6
scipy==1.17.1
scikit-learn==1.8.0
xgboost==3.2.0
joblib==1.5.3

# Eval config
PyYAML==6.0.3
